# agent-runtime v2: Digital employee lifecycle and soul engine
#
# v2 (2026-06-09):
#   ReasoningEngine → deprecated (v1 "brain")
#   ConnectorRouter → new (v2 dispatcher, dual-track)
#   SoulSerializer / MemoryService / EventBus / SandboxManager → new services
